# 手眼标定实现流程详解

## 1. 问题定义

手眼标定的核心问题是求解 **AX = XB** 方程。

其中:

| 符号 | 含义 | 维度 |
|------|------|------|
| **A_i** | 机器人末端执行器在两次姿态间的相对运动 | 4×4 齐次变换 |
| **B_i** | 相机观测到的标定板在两次姿态间的相对运动 | 4×4 齐次变换 |
| **X** | 待求的手眼变换矩阵 | 4×4 齐次变换 |

从 N 个机器人姿态中可构造 N-1 组相对运动对 (A_i, B_i)。

---

## 2. 两种标定模式

### 2.1 Eye-in-Hand (眼在手上)

```
相机固连在末端, 标定板固定。

    T_base^ee_i · X · T_cam_i^target = T_base^target  (常数)

A_i = (T_base^ee_{j})⁻¹ · T_base^ee_i        (机器人运动)
B_i = T_cam_i^target · (T_cam_j^target)⁻¹     (相机观测的相对运动)
X   = T_ee^cam                                 (相机在末端坐标系下的位姿)
```

### 2.2 Eye-to-Hand (眼在手外)

```
相机固定, 标定板固连在末端。

    X · T_base^ee_i · T_ee^target = T_cam_i^target

A_i = T_base^ee_j · (T_base^ee_i)⁻¹           (机器人运动)
B_i = (T_cam_j^target)⁻¹ · T_cam_i^target     (相机观测的相对运动)
X   = T_cam^base                               (相机在世界坐标系下的位姿)
```

> **注意**: 两种模式下 A_i, B_i 的定义不同, 但都满足 AX=XB。

---

## 3. 标定流程图

```
┌─────────────────────────────────────────────────────┐
│  1. 数据采集                                         │
│  - 移动机器人到 N 个不同姿态                          │
│  - 记录每帧的: T_base^ee (机器人运动学正解)            │
│  - 拍摄标定板, 解算 T_cam^target (PNP/标定板检测)      │
└─────────────────┬───────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────────────────┐
│  2. 数据预处理                                       │
│  - 对 N 个姿态, 构造 N-1 组相对运动对 (A_i, B_i)       │
│  - 根据 Eye-in-Hand / Eye-to-Hand 选择合适的公式        │
│  - 可选: 剔除运动量过小的姿态对 (旋转 < 5°, 平移 < 2cm)  │
└─────────────────┬───────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────────────────┐
│  3. 求解 AX=XB                                      │
│  ┌──────────────────────────────────────┐            │
│  │  Tsai-Lenz:  轴角分离, 先解旋转再解平移 │            │
│  │  Navy:       对偶四元数 + SVD 同时求解  │            │
│  │  Park:      李代数线性化              │            │
│  └──────────────────────────────────────┘            │
│   → 输出 X = [[R_X, t_X], [0, 1]]                   │
└─────────────────┬───────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────────────────┐
│  4. 结果验证                                         │
│  - 计算残差: ||A_i@X - X@B_i|| (每对数据)             │
│  - 如有真值: 计算旋转误差 & 平移误差                   │
│  - 在实际场景中测试: 机器人抓取物体验证                 │
└─────────────────────────────────────────────────────┘
```

---

## 4. 三种求解算法详解

### 4.1 Tsai-Lenz 方法 (1989)

**思想**: 将旋转和平移分离, 两步求解。

**旋转求解**:

定义修正 Rodrigues 参数:
```
P = 2·sin(θ/2)·axis
```

从旋转方程 R_A · R_X = R_X · R_B 可推导:
```
skew(P_A + P_B) · P_X = P_A - P_B
```

堆叠多组 (A_i, B_i) 得到超定方程组:
```
C · P_X = d
→ P_X = (CᵀC)⁻¹Cᵀd  (最小二乘)
```

然后从 P_X 反算 R_X:
```
|P_X| = 2·sin(θ/2) → θ
axis = P_X / |P_X|
```

**平移求解**:

已知 R_X 后:
```
(R_A - I)·t_X = R_X·t_B - t_A
```
堆叠成超定方程组, 最小二乘求解 t_X。

**优点**: 直观、经典, 被广泛引用。
**缺点**: 误差传播 — 旋转误差会影响平移精度。

### 4.2 Navy 方法 (Daniilidis, 1999)

**思想**: 使用对偶四元数统一表示旋转和平移, SVD 同时求解。

对偶四元数: `q̂ = q_r + ε·q_d`
- `q_r` (实部) 编码旋转
- `q_d` (对偶部) 编码平移: `q_d = 1/2 · q_t ⊗ q_r`

构建约束矩阵 T (4N × 4), 使得:
```
T · u = 0
```
其中 u 是编码了 R_X 的向量。

对 T 做 SVD, 取最小奇异值对应的右奇异向量作为旋转解。
平移由对偶部分的关系导出。

**优点**: 同时求解, 避免误差传播。
**缺点**: 实现较复杂, 对噪声敏感时可能不如 Tsai 稳定。

### 4.3 Park 方法 (1994)

**思想**: 对旋转方程取对数 (SO(3) → so(3)), 旋转和平移解耦。

取对数:
```
log(R_A) = R_X · log(R_B) · R_Xᵀ
```

记 `α = log(R_A)`, `β = log(R_B)` (旋转向量):
```
α = R_X · β
```

最小化 `||α - R_X·β||²`:
```
M = Σ β_i · α_iᵀ
R_X = (M·Mᵀ)⁻½ · M  (通过 SVD)
```

平移求解与 Tsai 相同。

**优点**: 旋转平移完全解耦, 数值优雅。
**缺点**: 需要对每个旋转矩阵取对数, 边界条件需处理。

---

## 5. 代码结构

```
handeye/
├── pyproject.toml        # 项目配置, 依赖管理 (uv)
├── README.md             # 项目说明
├── CALIBRATION_FLOW.md   # 本文档 — 详细实现流程
├── src/                  # 库函数
│   ├── __init__.py       # 包入口, 统一导出
│   ├── core.py           # 核心标定算法 (Tsai, Navy, Park)
│   ├── transform.py      # 旋转矩阵/四元数/轴角转换工具
│   ├── data.py           # 合成数据生成 & CalibrationData 数据结构
│   ├── io.py             # 数据加载/保存
│   └── visualize.py      # 误差可视化 & 坐标系绘图
├── scripts/              # 可执行演示脚本
│   ├── demo_eye_in_hand.py
│   ├── demo_eye_to_hand.py
│   └── generate_synthetic_data.py
└── outputs/              # 输出结果 (图片、数据文件)
```

---

## 6. 关键数学工具

### 齐次变换矩阵

```
T = [[R₃ₓ₃,  t₃ₓ₁],
     [0,    1   ]]

逆矩阵: T⁻¹ = [[Rᵀ,  -Rᵀ·t],
               [0,      1  ]]
```

### 旋转矩阵 → 轴角

```
θ = arccos((trace(R) - 1) / 2)
axis = (R - Rᵀ)₍₂,₁₎,₍₀,₂₎,₍₁,₀₎ / (2·sin(θ))
```

### 四元数 → 旋转矩阵

```
q = (w, x, y, z)

R = [[1-2y²-2z²,  2xy-2wz,    2xz+2wy  ],
     [2xy+2wz,    1-2x²-2z²,  2yz-2wx  ],
     [2xz-2wy,    2yz+2wx,    1-2x²-2y²]]
```

---

## 7. 实用建议

1. **姿态数量**: 最少 3 个 (产生 2 对), 建议 10+ 个
2. **运动多样性**: 纯平移不提供旋转信息, 需包含足够旋转
3. **运动幅度**: 每次运动应足够大 (旋转 >10°, 平移 >5cm)
4. **噪声处理**: 多次测量取平均, 或使用 RANSAC 剔除异常值
5. **方法选择**: 一般 Tsai 足够; 追求精度可尝试 Park; Navy 提供另一种视角
6. **实测验证**: 标定后让机器人移动到某已知目标点, 验证精度

---

## 8. 参考文献

- Tsai, R. Y., & Lenz, R. K. (1989). "A new technique for fully autonomous and efficient 3D robotics hand/eye calibration." *IEEE Trans. Robotics and Automation*, 5(3), 345-358.
- Daniilidis, K. (1999). "Hand-eye calibration using dual quaternions." *The International Journal of Robotics Research*, 18(3), 286-298.
- Park, F. C., & Martin, B. J. (1994). "Robot sensor calibration: solving AX=XB on the Euclidean group." *IEEE Trans. Robotics and Automation*, 10(5), 717-721.
